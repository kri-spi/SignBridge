import { Text, View } from "react-native";

export default function SpeechHandler() {
	return (
		<View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
			<Text>SpeechHandler route</Text>
		</View>
	);
}
